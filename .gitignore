__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

.venv/
venv/

.env

# Data (large / generated)
data/input/*
!data/input/.gitkeep
data/output/*
!data/output/.gitkeep
data/evidence/*
!data/evidence/.gitkeep
data/bari.db
*.db

# ML
ml/datasets/raw/*
!ml/datasets/raw/.gitkeep
ml/datasets/processed/*
!ml/datasets/processed/.gitkeep
runs/
*.pt
*.onnx
!ml/export/*.md
# Deployed model weights (committed deliberately — needed for the live
# Render deployment to actually run detection; see docs/deployment.md)
!ml/models/pothole_yolo_best.pt

# OS
.DS_Store
Thumbs.db

# Build artifacts / local tooling (regenerable, not source)
/BARI-Collector.apk
BARI_Interview_Prep.pdf
# dashboard/static/BARI-Collector.apk is a deliberate exception (see
# ml/models/pothole_yolo_best.pt above for the same pattern) — it's what
# both the local dashboard and the Render deployment serve for download.
*_log.txt
*_log[0-9]*.txt
gh_auth_log*.txt

# Stock pretrained weights (auto-downloaded by ultralytics on first use)
yolov8n.pt
